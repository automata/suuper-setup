# Suuper Setup

Setup a Linux box with all AI coding tools.

- `setup.sh`: For when setting up the Linux box
- `post_install.sh`: For when already got the Linux box running

# Install

```
curl -fsSL http://suuper.space:5000/suuper-setup/post_install.sh | bash
```